"""    :copyright: © 2019 by the Lin team.    :license: MIT, see LICENSE for more details."""from app.app import create_appfrom tests.utils import get_tokenfrom pprint import pprintapp = create_app()def test_authority():    with app.test_client() as c:        rv = c.get('/cms/admin/authority', headers={            'Authorization': 'Bearer ' + get_token()        })        json_data = rv.get_json()        pprint(json_data)        assert rv.status_code == 200# def test_delete_user():#     with app.test_client() as c:#         rv = c.delete('/cms/admin/6', headers={#             'Authorization': 'Bearer ' + get_token()#         })#         json_data = rv.get_json()#         pprint()(json_data)#         assert rv.status_code == 201def test_get_admin_users():    with app.test_client() as c:        rv = c.get('/cms/admin/users', headers={            'Authorization': 'Bearer ' + get_token()        })        json_data = rv.get_json()        pprint(json_data)        pprint(len(json_data['collection']))        assert rv.status_code == 200def test_change_user_password():    with app.test_client() as c:        rv = c.put('/cms/admin/password/2', headers={            'Authorization': 'Bearer ' + get_token()        }, json={            'new_password': 'zcxyun', 'confirm_password': 'zcxyun'        })        json_data = rv.get_json()        pprint(json_data)        assert rv.status_code == 201def test_get_admin_groups():    with app.test_client() as c:        rv = c.get('/cms/admin/groups', headers={            'Authorization': 'Bearer ' + get_token()        })        json_data = rv.get_json()        pprint(json_data)        assert rv.status_code == 200def test_get_all_group():    with app.test_client() as c:        rv = c.get('/cms/admin/group/all', headers={            'Authorization': 'Bearer ' + get_token()        })        json_data = rv.get_json()        pprint(json_data)        assert rv.status_code == 200def test_get_group():    with app.test_client() as e:        rv = e.get('/cms/admin/group/1', headers={            'Authorization': 'Bearer ' + get_token()        })        json_data = rv.get_json()        pprint(json_data)        assert rv.status_code == 200